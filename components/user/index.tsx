export default function User() {
  // 유저 정보 불러옴
  // const userId = cookies().get('userId')?.value;
  // let cart;

  // if (userId) {
  //   cart = await getUserData(userId);
  // }

  return <div>마이페이지</div>;
}
